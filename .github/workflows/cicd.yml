name: 'CI/CD'
on:
  push:
    branches: ['master']
  pull_request:
    branches: ['master']

jobs:
  # test:
  #   runs-on: 'ubuntu-latest'
  #   steps:
  #     - uses: actions/checkout@v2
  #     - run: npm ci
  #     - run: npm run build --if-present
  #     - run: npm test
  build:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: actions/checkout@v2
      - run: npm ci
      - run: npm run build --if-present
      - run: npm run deploy
